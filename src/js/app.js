import "../scss/app.scss";
function myFunction() {
  document.getElementsByClassName(".product").setAttribute("data-price"); 
}
window.addEventListener("DOMContentLoaded", () => {
  // This block will be executed once the page is loaded and ready


});
